server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve SPA routes
    location / {
        try_files $uri /index.html;
    }

    # Optional: static asset caching
    location /assets/ {
        expires 1y;
    }
}

